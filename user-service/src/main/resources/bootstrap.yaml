spring:
  application:
    name: user-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
    vault:
      application:
        name: user-service
      scheme: http
      authentication: TOKEN
      host: localhost
      token: myroot
      port: '8200'